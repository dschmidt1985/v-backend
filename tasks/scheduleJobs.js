module.exports = function (server, restify) {
  var schedule = require('node-schedule');// https://www.npmjs.com/package/node-schedule

  // ist mehr etwas wenn etwas zu einem bestimmten Zeitpunkt ausgeführt werden soll. bsp. pushs...


  // var j = schedule.scheduleJob({second: 5}, function () {
  //   console.log('The answer to life, the universe, and everything!' + new Date().getTime());
  // });

};